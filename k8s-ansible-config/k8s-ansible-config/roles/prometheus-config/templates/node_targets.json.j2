[
  {
    "targets": [
{% for host in groups['all'] %}
      "{{ hostvars[host].ansible_host | default(host) }}:9100"{% if not loop.last %},{% endif %}

{% endfor %}
    ],
    "labels": {
      "job": "eks-worker-nodes"
    }
  }
]
